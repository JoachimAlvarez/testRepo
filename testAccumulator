quote location: :keep do      
  # some dependencies
  import DevOpsCore.SlaveModule

  x = 0

  Enum.each Enum.to_list(1..999999), fn x ->
    # reset X sometimes
    x = if x > 100 do
      0
      else
      x
    end
    return x
    
    x = x+1
    
    :timer.sleep 1000
  end 
end
