quote location: :keep do      
  # some dependencies
  import DevOpsCore.SlaveModule
  
  Enum.each Enum.to_list(0..999999), fn x ->
    return rem(x*5, 101)
    
    # return (:rand.uniform(50)+130)
    
    :timer.sleep 1000
  end 
end
