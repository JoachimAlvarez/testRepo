quote location: :keep do      
  # some dependencies
  import DevOpsCore.SlaveModule

  x = 0

  Enum.each Enum.to_list(1..999999), fn _x ->
    return x
    
    # update X
    x = x+1
    
    # reset X?
    if x > 100 do
        x = 0
    end
      
    
    
    :timer.sleep 1000
  end 
end
