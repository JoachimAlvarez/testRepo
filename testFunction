quote location: :keep do      
  # some dependencies
  import DevOpsCore.SlaveModule

  return :start

  Enum.each Enum.to_list(1..999999), fn x ->
    return :TEST
    
    :timer.sleep 500
  end 

  return :stop
end
