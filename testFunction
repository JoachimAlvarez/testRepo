quote location: :keep do      
  # some dependencies
  import DevOpsCore.SlaveModule

  value = :githubTEST

  return :start

  Enum.each Enum.to_list(1..99999), fn x ->
    return value
    :timer.sleep 500
  end 

  return :stop
end
