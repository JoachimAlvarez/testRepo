quote location: :keep do      
  # some dependencies
  import DevOpsCore.SlaveModule

  return :start

  Enum.each Enum.to_list(1..999999), fn x ->
    # return :TEST
    return :rand.uniform(100)
    
    :timer.sleep 500
  end 

  return :stop
end
